import React from "react";
import { Container, Alert } from "reactstrap";

const Success = () => (
   <Container>
      <br />
      <Alert color="success">Details Succesfully Saved</Alert>
   </Container>
);

export default Success;
